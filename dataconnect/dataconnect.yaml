specVersion: "v1"
serviceId: "screens"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "screens-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
